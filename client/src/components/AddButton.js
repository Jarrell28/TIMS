import { Button } from 'reactstrap';

const AddButton = (props) => {
    return (
        <div>
            <Button color="secondary">Check Out Item</Button>{' '}
        </div>
    );
}

export default AddButton;